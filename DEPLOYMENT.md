# GitHub Pages Deployment

This repository deploys Camille's Finance automatically from `main` through `.github/workflows/deploy-pages.yml`.

Expected site URL: `https://brando2moon.github.io/BudgetHome/`

The workflow verifies the application before publishing the static site.
