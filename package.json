{
  "name": "camilles-finance-pixel-bank",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check src/budgetEngine.js",
    "verify": "npm test && npm run check"
  }
}
