{
  "name": "camilles-finance-pixel-bank",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check app.js && node --check animation-v9.js && node --check src/budgetEngine.js && node --check src/animationEngine.js",
    "verify": "npm test && npm run check"
  }
}
